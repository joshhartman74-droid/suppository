# PDF Page Filter with Mistral OCR - Requirements
# Install with: pip install -r requirements.txt

# Core dependencies
pypdf>=3.0.0
pdf2image>=1.16.0
Pillow>=9.0.0
httpx>=0.23.0
python-multipart>=0.0.5

# Optional: For better OCR (alternative to Mistral)
# pytesseract>=0.3.10

# For parallel processing
concurrent-log-handler>=0.9.25